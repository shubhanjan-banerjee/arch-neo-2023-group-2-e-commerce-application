apiVersion: v1
kind: Service
metadata:
  name: voting-app-service
  namespace: default
  labels:
    name: voting-app-service
    app: ecommerce-app
spec:
  type: NodePort
  selector:
    name: voting-app-pod
    app: ecommerce-app
  ports:
  - targetPort: 80
    port: 80
    nodePort: 30008
